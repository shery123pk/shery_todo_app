# Database (Neon PostgreSQL with asyncpg driver)
# Get from: https://console.neon.tech
# IMPORTANT: Must use postgresql+asyncpg:// (not just postgresql://)
# For asyncpg, use ssl=require (not sslmode=require)
DATABASE_URL=postgresql+asyncpg://user:password@host.neon.tech/dbname?ssl=require

# JWT Secret (generate with: openssl rand -hex 32)
JWT_SECRET=your-secret-key-here-change-in-production

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3004

# Email Service (SendGrid or SMTP)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USERNAME=apikey
SMTP_PASSWORD=your-sendgrid-api-key
FROM_EMAIL=noreply@example.com

# Environment
ENVIRONMENT=development

# File Upload
MAX_FILE_SIZE_MB=10
UPLOAD_DIR=./uploads

# Email Notifications for Task Reminders (Optional)
# For Gmail: Use App Password (https://myaccount.google.com/apppasswords)
# For other email providers: Use regular SMTP credentials
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-gmail-app-password
FROM_EMAIL=your-email@gmail.com
